# 2026-04-17 (10) — игра 5 букв: 5 SHC за победу, чистка модалки, отключение виджета

## Что сделано

1. **Награда в игре «5 букв» повышена с 3 SHC → 5 SHC** за победу (лимит 3 победы/день сохранён, максимум 15 SHC/день).
2. **GamesModal (src/config/games.js)**:
   - Убран пункт «Играть могут только пользователи с активной подпиской» (избыточно — /game и так доступна только подписчикам).
   - Убрано упоминание «(сброс в 22:10 по калининградскому времени)». Оставлено «Новый день — новое слово».
   - Добавлен пункт: «SHC можно потратить до 100% стоимости любого заказа (роллы, сеты и любая другая продукция)».
   - `reward` обновлён до «5 SHC за каждую победу (до 3 побед в день)».
3. **WordlePage.js**: тексты «3 SHC» → «5 SHC», добавлена фраза про 100% оплату в locked-экран.
4. **FloatingGameWidget полностью отключён** — убран импорт из App.js и все 5 вставок. Файл `src/components/FloatingGameWidget.js` оставлен в репо (можно удалить позже, если не вернём).

## Изменённые файлы

| Файл | Правка |
|------|--------|
| `api/_lib/db.js:865` | `updateBalance(telegramId, 3)` → `5` в `recordGameWin` |
| `api/_lib/db-pg.js:752` | `updateBalance(telegramId, 3)` → `5` в `recordGameWin` |
| `api/game-guess.js:80` | `shcEarned ? 3 : 0` → `? 5 : 0` |
| `src/WordlePage.js` | два текста: locked-экран + бейдж бонусов |
| `src/config/games.js` | reward + conditions |
| `src/App.js` | удалён импорт FloatingGameWidget и все его JSX-вставки |

## Коммит

`6e59cb7` — feat(game): 5 SHC за победу + чистка модалки + отключение виджета

## Деплой

Запушено на main. На VPS деплой **не выполнен** в этой сессии — пользователь запустит вручную:
```
cd miniapp-sushii && git pull && docker compose up -d --build && docker compose restart app
```

## Открытые вопросы

- Удалять ли `src/components/FloatingGameWidget.js` физически? Пока оставлен.
- Bump версии до v1.0.1 — не делал, пользователь не просил.
