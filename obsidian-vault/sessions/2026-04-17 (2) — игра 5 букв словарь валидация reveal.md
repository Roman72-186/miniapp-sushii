# 2026-04-17 (2) — Игра «5 букв»: словарь, валидация, reveal-баннер

## Что сделано

### Словарь 500+ слов
- `api/game-words.json` расширен с 35 до 500+ слов
- **Только существительные** — именительный падеж, распространённые слова
- Покрывает весь русский алфавит (А–Я)

### Валидация слова по словарю
- Добавлена `getGameWordExists(word)` в `api/_lib/db.js` (SQLite) и `api/_lib/db-pg.js` (PG async)
- В `api/game-guess.js` после regex-проверки: если слова нет в словаре → 400 `«Такого слова нет в словаре»`
- Импорт: `getGameWordExists` добавлен в require

### Reveal-баннер при проигрыше
- `src/hooks/useWordle.js`: добавлен параметр `onGameOver`; вызывается при победе (с winsToday) и при проигрыше (с null)
- Тост при проигрыше теперь просто `«Не угадали 😔»` — без слова
- `src/WordlePage.js`: `handleGameOver` колбэк + `revealedWord` / `gameWon` деструктурированы из hook
- Reveal-баннер рендерится под сеткой: красный фон, буквы с разрядкой

### Стили
- `src/wordle.css`: добавлены `.wrd-reveal-banner`, `.wrd-reveal-banner__label`, `.wrd-reveal-banner__word`

## Изменённые файлы

| Файл | Изменения |
|------|-----------|
| `api/game-words.json` | 35 → 500+ слов |
| `api/_lib/db.js` | +getGameWordExists |
| `api/_lib/db-pg.js` | +getGameWordExists (async) |
| `api/game-guess.js` | валидация по словарю |
| `src/hooks/useWordle.js` | onGameOver колбэк |
| `src/WordlePage.js` | handleGameOver + reveal-баннер JSX |
| `src/wordle.css` | .wrd-reveal-banner стили |

## Коммит

`ccc68de` — feat: игра 5 букв — словарь 500+ слов, валидация, reveal-баннер

## Открытые вопросы

- На проде при первом старте словарь seed-ится только если таблица пустая — если слов уже 35, новые слова не добавятся автоматически. Нужен ручной скрипт миграции словаря на VPS.
