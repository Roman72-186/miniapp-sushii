# 2026-04-15 (2) — /init CLAUDE.md, деплой

## Что сделано

### 1. Улучшение CLAUDE.md (`/init`)
Запущена команда `/init` — анализ кодовой базы и точечное обновление существующего `CLAUDE.md`.

Три улучшения:
- **UpsellBlock**: добавлено важное условие — `onAddItem` обязательно должен передаваться из родительской страницы (баг был именно в этом в коммитах `86c28d6`/`a97da76`); уточнено что «◈ Допродажи» — отдельная вкладка AdminPage.
- **SHC Balance**: явно помечено что `processCommissions()` **намеренно отключена** — было только в memory, отсутствовало в CLAUDE.md (опасный пробел).
- **shop-v2.css**: добавлено явное правило — дизайн магазина менять только в `shop-v2.css`, не трогать JSX.

### 2. Деплой
- Коммит `f00f4f8` — docs: обновить CLAUDE.md + upsell.json, новые сессии и скрипты
- Из индекса исключены чувствительные файлы с данными пользователей (`data/db-users-*.csv/json`, `data/watbot-link-*.json`)
- На VPS конфликт: `scripts/*.js` существовали как untracked — удалены, pull прошёл
- Docker build: Compiled successfully, контейнер пересобран

## Файлы изменены
- `CLAUDE.md` — три точечных улучшения
- `data/products/upsell.json` — закоммичен (список SKU допродаж)
- `obsidian-vault/sessions/` — добавлены все накопившиеся сессии

## Коммиты
- `f00f4f8` — docs: обновить CLAUDE.md + upsell.json, новые сессии и скрипты

## Открытые вопросы
Нет.
