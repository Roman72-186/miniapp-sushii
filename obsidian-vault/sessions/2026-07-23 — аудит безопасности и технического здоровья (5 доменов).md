# Аудит безопасности и технического здоровья — 2026-07-23

Пять параллельных субагентов разобрали код по зонам риска. Ниже — полный список находок с указанием, что уже исправлено в этой сессии, а что осталось на потом.

Связано: [[SQLite как основное хранилище]], [[файловый кэш пользователей — зачем и как]]

---

## Статус исправлений

### ✅ Исправлено 2026-07-23

1. **`api/export-contacts.js`** — публичный дамп всей базы клиентов без авторизации → добавлена проверка admin-токена (`checkAuth`).
2. **`api/yookassa-webhook.js`** — подмена `X-Forwarded-For` обходила проверку подлинности вебхука; тело запроса использовалось без верификации → убрана проверка по IP, платёж теперь **всегда** верифицируется через API YooKassa по `payment.id`, вся дальнейшая логика берёт данные только из проверенного ответа.
3. **Системное отсутствие JWT-проверки** на 15 пользовательских эндпоинтах (`get-profile`, `get-order-history`, `get-transactions`, `get-gift-history`, `get-referrals`, `claim-gift`, `cancel-subscription`, `register-referral`, `apply-partner-code`, `user-order-rating`, `check-vip`, `sync-user`, условно `create-order`/`create-payment` с сохранением анонимного гостевого чекаута) → identity теперь везде берётся из `req.userId` (JWT), а не из тела запроса.

Легаси-путь идентификации по числовому `telegram_id` без JWT (Telegram WebApp + URL-параметр `?telegram_id=` в `UserContext.js`) сознательно не восстанавливался — по решению владельца эти пользователи больше не активны. Побочный эффект: `CompleteRegistrationPage.js` (флоу завершения веб-регистрации легаси-юзеров) теперь получает 401 от `get-profile` вместо профиля.

`check-vip.js` защищён JWT, но там остался отдельный не связанный баг: для веб-пользователей `telegram_id` — строка `web_xxx`, а не настоящий числовой Telegram ID, поэтому проверка членства в VIP-группе и раньше не работала для них корректно. Не трогали — отдельная задача.

4. **Двойной запуск крона подписок без блокировки** — встроенный `setTimeout`-крон в `server.js` и внешний `/api/cron-subscriptions` работают в одном Node-процессе (single-instance деплой, без реплик в `docker-compose.yml`) → добавлен in-memory лок `isCronRunning` в `runSubscriptionCron()` (`api/cron-subscriptions.js`): при параллельном вызове второй просто пропускается (`{ skipped: true }`) вместо повторного списания/деактивации.
5. **Захват аккаунта через email-OTP** — при перепроверке масштаб оказался шире формулировки из первичного отчёта: `set-password.js` перезаписывает пароль безусловным `upsert` по телефону, а кнопка «Забыли пароль?» на фронтенде (`LoginPage.js`) доступна и для аккаунтов с уже установленным паролем — значит, зная чужой телефон, можно было перехватить не только аккаунты без пароля, но и любой существующий аккаунт целиком (сброс пароля через произвольный email). Фикс — единая точка входа: `send-email-otp.js` теперь шлёт код только на email, уже привязанный к номеру (если он есть); если email отличается — 403. `verify-otp.js` и `set-password.js` не трогали — код к атакующему больше не попадёт до них. Для аккаунтов без email и пароля (первичная привязка, легаси-регистрация через `CompleteRegistrationPage.js`) поведение не изменилось — это отдельный, не закрытый архитектурный пробел: в проекте нет SMS-верификации телефона вообще, полноценно закрыть первичную привязку без неё нельзя.
6. **Утечка API-ключа TestSprite в git-историю** (`opencode.jsonc`, коммит `827715f`) — при повторной проверке оказалось хуже, чем в первичном отчёте: ключ был открыт не только в истории, а прямо в текущем `HEAD`, вместе со вторым, ранее не замеченным ключом Context7, в **публичном** GitHub-репозитории (`Roman72-186/miniapp-sushii`).
   - Оба ключа вынесены в `{env:TESTSPRITE_API_KEY}` / `{env:CONTEXT7_API_KEY}` в `opencode.jsonc`, значения перенесены в локальный `.env` (гитигнорится), плейсхолдеры добавлены в `.env.example`.
   - Заодно запушены на `origin/main` 4 накопившихся локальных коммита (включая сегодняшний `ae6ec87` — до этого фиксы дампа базы/вебхука/JWT существовали только локально, на GitHub их не было).
   - Вся git-история переписана через `git filter-repo --replace-text` (секреты заменены на `***REMOVED-...-KEY***` во всех коммитах/ветках), сделан `force-push origin main`. Проверено `git grep` по всем ревизиям — совпадений 0. Ветка с открытым PR (`feature/set-images-and-gift-restrictions`) не содержала эти коммиты в предках — не задета, форков репозитория — 0.
   - **Сами значения ключей всё ещё действительны** — переписывание истории и вынос в env только скрывают их из репозитория, отозвать может только владелец в личных кабинетах TestSprite и Context7. Это не сделано, ключи нужно ротировать при первой возможности.
7. **Деплой на VPS был просрочен** — при попытке задеплоить коммиты п.1-6 обнаружилось, что `git pull` на сервере ломается: история на VPS ссылалась на дохэшевые коммиты (до `filter-repo` из п.6), плюс в рабочей директории лежали незакоммиченные ручные хотфиксы (`getPaymentByYooKassaId` для идемпотентности вебхука, `findStoreForDelivery` для зон доставки, рефактор кэша при отмене подписки). Построчная сверка (`diff -b`) показала: все эти хотфиксы уже присутствуют в локальном git байт-в-байт — только `.env.example` терял одну безвредную строку-комментарий. Значит **прод всё это время работал на старом уязвимом коде** — фиксы X-Forwarded-For в вебхуке и JWT-проверки на `create-order.js`/`cancel-subscription.js` были только в git, не в проде. Сделано: `git reset --hard origin/main` на VPS (untracked-мусор — `.bak`-файлы, `migrate.js`, старые экспорты — не тронут), `docker compose up -d --build`, здоровье подтверждено (`200` на `sushi-house-39.ru`, чистые логи).
8. **`JWT_SECRET` на проде был захардкоженной заглушкой** — при проверке логов после деплоя всплыло предупреждение `[config] Внимание: используйте надежный JWT_SECRET`. Сверка подтвердила: значение в `.env` на VPS **дословно совпадало** с публичной заглушкой из `api/_lib/auth.js`/`server.js` (`your-super-secret-jwt-key-change-this-in-production-...`), которая открыта в публичном GitHub-репозитории. Это обнуляло все JWT-проверки из п.3 и п.7 — токен на любой аккаунт мог подписать кто угодно, зная только код. Сгенерирован новый случайный секрет (64 base64url-символа), записан в `.env` на VPS (старый `.env` сохранён как `.env.bak-before-jwt-rotation-<дата>`), контейнер пересоздан (`docker compose up -d --force-recreate app` — `restart` не подхватывает новый `env_file`, только пересоздание). Побочный эффект: все активные пользовательские сессии разлогинены, это ожидаемо и подтверждено с владельцем перед выполнением.
9. **Гонка идемпотентности вебхука YooKassa** — код-проверка `getPaymentByYooKassaId` перед вставкой не защищала от гонки: два параллельных экземпляра вебхука могли одновременно пройти `SELECT` до того, как любой из них вставит запись, и оба продлевали подписку, начисляли SHC и создавали заказ во Frontpad. Добавлен `UNIQUE`-индекс `idx_payments_yookassa_unique` на `payments.yookassa_payment_id` (`api/_lib/db.js`, `api/_lib/db-pg.js` — миграции для существующих баз; `scripts/create-pg-schema.sql`, `scripts/setup-pg-schema.js` — для новых). В `api/yookassa-webhook.js` вставка платежа (`recordPayment`) перенесена в самое начало обработки — до создания заказа во Frontpad, продления подписки и начисления SHC; при перехвате ошибки уникальности (`SQLITE_CONSTRAINT_UNIQUE` / Postgres `23505`) запрос останавливается и возвращает `already_processed`, не выполняя побочные эффекты повторно. Старый `SELECT`-check оставлен как быстрый путь для типичного последовательного повтора, но не является защитой от гонки — теперь это делает `UNIQUE`-индекс.
   - **При деплое выяснилось: прод работает не на SQLite, а на Postgres/Supabase** (`USE_SUPABASE=true`) — `CREATE UNIQUE INDEX` сразу же упал с конфликтом на существующих данных. Расследование показало 87 дублирующихся пар платежей (174 из 359 строк) за 2026-05-02 — 2026-07-18 — исторический след уже закрытого бага двойного запуска крона (см. п.4 выше) в связке с этой же гонкой идемпотентности. Деньги с карт клиентов дублирование не списывало (`yookassa_payment_id` в паре совпадает — в YooKassa это один и тот же реальный платёж), но найдены реальные побочные эффекты:
     - **+1910 SHC лишних одному пригласившему** (15 случаев задвоенного начисления реферальной комиссии);
     - **22 подтверждённых случая двойного продления подписки** (следующее автосписание сдвинулось на 60 дней вместо 30) — примерная упущенная выручка **≈13 180 ₽**;
     - двойные заказы во Frontpad за эти же события вероятны, но не проверялись (нет доступа к истории заказов Frontpad из этой сессии).
     - 63 дублирующиеся пары не имеют следующего платежа после себя — однозначно связать с багом нельзя (могли не продлить подписку по своим причинам).
   - **Владелец подтвердил дедуп с бэкапом** (без исправления SHC-баланса и упущенной выручки — это отдельное открытое решение, см. «Не исправлено — важно» ниже). Сделано: полный бэкап всех 174 строк-дублей в JSON (`/root/payments-dupes-backup-2026-07-23.json` на VPS вне контейнера), удалена более поздняя строка из каждой пары (87 строк, оставлена самая ранняя по `created_at`), после чего `UNIQUE`-индекс успешно создан. Здоровье прода подтверждено (`200`, `272` строк в `payments` после дедупа = `359 − 87`).
10. **Перегруженное поле `tariff`** — разовая покупка амбассадорского тарифа (9990) безусловно перезаписывала `tariff` пользователя в БД (`upsertUser` в `api/yookassa-webhook.js`), даже если у него уже была активная подписка на другой тариф. Дальше `api/cron-subscriptions.js` (`getRecurringAmount(user.tariff)`) брал это же поле для суммы автосписания — следующее продление списало бы цену амбассадорского тарифа (9990 ₽) вместо реальной цены подписки. Тот же паттерн дублировался в блоке обновления файлового кэша (`resolvedTarif`) чуть ниже в том же файле — влияет на `get-gift-windows.js`/`_lib/subscription-state.js`, которые читают `cache.tarif` для проверки окон подарков. Фикс: `tariff` (и `resolvedTarif` в кэше) не перезаписывается разовой покупкой, если у пользователя `subscription_status === 'активно'` — для пользователей без подписки (чистая разовая покупка амбассадорства) поведение не изменилось, бейдж «АМБА» на `ProfilePage.js` работает как раньше. Только код, деплой на VPS не выполнялся.
11. **Гонка при списании SHC-баллов** — в `api/create-order.js` проверка баланса (`SELECT`) и списание (`updateBalance`) были разнесены во времени целым внешним HTTP-вызовом к Frontpad между ними; `updateBalance` в `api/_lib/db.js`/`db-pg.js` делала `UPDATE ... SET balance_shc = balance_shc + ?` без условия `>= 0`. Два параллельных запроса (двойной клик, повтор) с одинаковым снимком баланса могли оба пройти проверку и оба списать — баланс уходил в минус, и оба запроса успевали создать реальный заказ во Frontpad со скидкой. Фикс: `updateBalance` теперь атомарна (`WHERE balance_shc + ? >= 0`) и возвращает `true`/`false` в зависимости от того, применилось ли списание; списание баллов в `create-order.js` перенесено на момент ДО вызова Frontpad (резервирование), при неудачном резерве запрос останавливается с 400 раньше создания заказа, а при отказе Frontpad баллы возвращаются обратно. Начисления (положительные `amount`, реферальные SHC, ручное admin-add-shc) не затронуты — условие `balance_shc + amount >= 0` для них всегда истинно. Только код, деплой на VPS не выполнялся.
12. **Таблица `orders` отсутствовала в PG-схема-скриптах** (`scripts/create-pg-schema.sql`, `scripts/setup-pg-schema.js`) — при развёртывании базы с нуля этими скриптами таблица `orders` не создавалась вообще. `api/_lib/db-pg.js` (`ensureMigrations()`) на такой базе делает `ALTER TABLE orders ADD COLUMN IF NOT EXISTS ...` (строки 40-44), что в Postgres падает, если самой таблицы нет — а `ensureMigrations()` оборачивает ВСЕ миграции в один общий `try/catch`, поэтому ошибка на `orders` обрывала выполнение и для всего, что шло следом в том же блоке: создание `email_notifications_log`, таблиц игры «Пять букв» и (на момент этой сессии) `UNIQUE`-индекса `idx_payments_yookassa_unique` из п.9. На текущем проде это не проявлялось, потому что `orders` там уже существует (создана раньше, вне этих скриптов) — баг чисто про «чистый» деплой с нуля. Фикс: `CREATE TABLE IF NOT EXISTS orders (...)` добавлена в оба скрипта, с колонками `promo_code`/`has_promo_gift`/`has_threshold_gift` сразу в определении таблицы (в db.js/SQLite они добавлены отдельными `ALTER TABLE`, здесь смысла в этом нет — таблица создаётся с нуля). Плюс индекс `idx_orders_telegram`. Только код, деплой на VPS не выполнялся (на проде и так работает, т.к. таблица уже есть).
    - **Смежный, отдельно не закрытый пробел, найденный при проверке:** `scripts/migrate-sqlite-to-pg.js` (одноразовый скрипт миграции SQLite → PG) вообще не упоминает `orders` — таблица заказов им не переносится. На проде это уже не актуально (миграция состоялась раньше, `orders` в PG живёт своей историей), но если её когда-нибудь понадобится прогнать заново (например, для отката или тестовой базы) — история заказов из SQLite не скопируется. Не трогал, вне запрошенного объёма правки.
13. **Сломанный прогресс-бар подписки в `ProfilePage.js`** — опечатка в регистре ключа: код читал `profile?.датаНАЧАЛА` (все буквы «АЧАЛА» заглавные), а везде в проекте (`sync-user.js`, `get-profile.js`, `get-gift-windows.js`, `_lib/subscription-state.js`, `yookassa-webhook.js`, `UserContext.js`) канонический ключ — `датаНачала` (только «Н» заглавная). Из-за несовпадения регистра `profile.датаНачала` всегда был `undefined`, `subProgress` всегда возвращал `null`, полоса прогресса подписки не рендерилась с момента написания фичи. Фикс — две строки (133, 135), регистр приведён к `датаНачала`. Только код, деплой не выполнялся.
14. **Промокод не сбрасывался после заказа** — в `src/DiscountShopPage.js` (`handleOrderSuccess`) после успешного оформления заказа корзина очищалась (`cart.clear()`), но состояние `promoCode` не сбрасывалось — при следующей покупке поле промокода в `CartPanel.js` оставалось заблокированным старым кодом (`isPromoApplied` оставался `true`). Тот же паттерн без отдельного упоминания в аудите нашёлся в `src/ShopPage.js` (идентичная `handleOrderSuccess`) — исправлен тоже. Фикс — `setPromoCode('')` после `cart.clear()` в обоих файлах. Только код, деплой не выполнялся.
16. **Оракул для перебора телефонов + отсутствие rate-limit** в `api/auth/login-by-phone.js` — ответ эндпоинта раскрывал незалогиненному вызывающему, зарегистрирован ли номер телефона (`isExistingUser`) и установлен ли для него пароль (`hasPassword`/`requiresEmail`), без какого-либо ограничения скорости. Само различие в ответе убрать без изменения UX-флоу входа нельзя — клиенту нужно знать, показывать поле пароля или запрашивать email для OTP (это законная часть продукта, не баг). Закрыл измеримую часть: добавлен `api/_lib/rate-limit.js` — простой in-memory rate-limiter (годится только для single-instance деплоя без реплик, тот же инвариант, что у `isCronRunning` в `cron-subscriptions.js`), лимиты — 10 запросов/10 мин на один номер телефона и 30 запросов/10 мин на один IP (`429` при превышении). IP берётся из заголовка `X-Real-IP`, который nginx выставляет сам как `$remote_addr` на всех проксируемых location — проверил `docker-compose.yml`: контейнер `app` объявлен через `expose` (только внутри docker-сети), наружу торчит только `nginx` (`ports: 80/443`) — прямого обхода nginx нет, значит `X-Real-IP` нельзя подделать снаружи (в отличие от уже закрытого бага с `X-Forwarded-For` в вебхуке YooKassa, где такой гарантии не было). Только код, деплой на VPS не выполнялся.
17. **Fallback `JWT_SECRET` захардкожен в коде** — `api/_lib/auth.js` и `api/update-profile.js` независимо друг от друга при отсутствии `process.env.JWT_SECRET` тихо подставляли строку `'your-secret-key-change-in-production'` и продолжали работать — сервер запускался и подписывал реальные токены публично известным (открытым на GitHub) значением вместо явного падения. Ровно этот класс бага уже привёл к живому инциденту в этой сессии раньше (см. п.8 в статусе исправлений выше — правда, там сработала ДРУГАЯ захардкоженная заглушка, буквально скопированная в `.env` на проде). Фикс: `api/_lib/auth.js` теперь бросает исключение при `require()` (задолго до `app.listen` — все `api/*.js`-роуты, включая этот модуль, требуются синхронно при старте `server.js`), если `JWT_SECRET` не задан **или** равен одной из двух известных строк-заглушек (`your-secret-key-change-in-production` из `_lib/auth.js`/`update-profile.js`, `your-super-secret-jwt-key-change-this-in-production-$(openssl rand -hex 32)` из `server.js`) — сервер не поднимется без настоящего секрета. `api/update-profile.js` больше не дублирует свою копию константы, импортирует `JWT_SECRET` из `_lib/auth.js`. Заодно убрана из `server.js` дублирующая проверка в `app.listen()` — она стала мёртвым кодом (до неё уже не дойти с плохим секретом). **Поведенческий эффект деплоя:** до этого фикса сервер запускался всегда (в худшем случае — с предсказуемым секретом и warning в логе); после фикса отсутствие/заглушка `JWT_SECRET` в `.env` останавливает запуск процесса целиком — это осознанный fail-fast, но при деплое стоит держать в уме. На проде `JWT_SECRET` уже ротирован на случайное значение (см. п.8), деплой этого фикса пройдёт штатно. Только код, деплой на VPS не выполнялся.

### ⏳ Не исправлено — критично

- **Сломанная вкладка «Баннеры» в `AdminPage.js`** — три независимых JSX-блока подряд под одним условием `tab === 'banners'` (строки ~1242, ~1291, ~1524): старая сетка баннеров, полная детальная панель подписчиков (явно предназначенная для вкладки «Люди»), актуальный редактор баннеров. Реально показывается смесь всех трёх.
- **Ротация ключей TestSprite и Context7** — код и история очищены (см. п.6 выше), но сами значения ключей ещё действительны. Нужно зайти в кабинеты провайдеров и отозвать/перевыпустить, затем вписать новые значения в `.env`.

### ⏳ Не исправлено — важно

- **Последствия исторических дублей платежей (найдено 2026-07-24, см. п.9 выше)** — не исправлялось намеренно, решение за владельцем: списать ли 1910 лишних SHC у затронутого пригласившего (15 задвоенных начислений) и что делать с ≈13 180 ₽ упущенной выручки от 22 случаев двойного продления подписки (60 дней вместо 30). Полный список затронутых `telegram_id` — в бэкапе `/root/payments-dupes-backup-2026-07-23.json` на VPS.
- Отсутствуют security-заголовки (HSTS, CSP, X-Frame-Options) и rate limiting в `nginx/default.conf` на auth/admin-эндпоинтах.
- Docker-контейнер (`Dockerfile`) запускается от root, нет `USER` директивы.
- `.dockerignore` не исключает `data/`, `*.db`, `opencode.jsonc`, `.secrets.lock.local`.
- Нет backup-стратегии для Docker volume `app-data`.

### ⏳ Технический долг — без спешки

- Дублирование логики нормализации телефона в трёх местах (`PaymentPage.js`, `ProfilePage.js`, `EditProfileModal.js`) вместо единого `src/utils/phone.js`.
- Рассинхрон написания названий товаров между каталогами (Ё/Е, Э/Е, дефисы) — ломает синхронизацию `enabled`-флага в `SYNC_GROUPS` (`api/admin-products.js`). Конкретные позиции: «Суши сэндвич/сендвич с лососем» (rolls/rolls-sub/rolls-490), «Запечённый/Запеченный Мидзуи» и ещё 3 позиции (zaproll/zaproll-sub), «Суши пицца №1-3» vs «Суши-пицца лосось/креветки/курица».
- Числовые PG-колонки возвращаются как строки без явного каста (`getMonthRevenue`, `getTotalEarnings` в `db-pg.js`) — выручка в админке отображается без разделителя разрядов из-за смены бэкенда (SQLite → PG).
- PII (телефон, адрес) в открытом виде в логах `create-order.js`.
- Неатомарная запись каталогов в `data/products/*.json` (`api/admin-products.js`, `api/_lib/gift-rules.js`) — в отличие от уже правильно сделанных `user-cache.js`/`blob-store.js` (tmp+rename).
- Отладочные `console.log` в горячих путях (`subscription-state.js`, `gift-windows.js`, `DiscountShopPage.js`, `sync-user.js`).
- Дублирование `formatDate` в `yookassa-webhook.js` и `cron-subscriptions.js`.
- Мёртвый экспорт `processCommissions` в `db.js`/`db-pg.js` (амбассадорские комиссии намеренно отключены).
- Refresh-токен (`generateRefreshToken` в `auth.js`) не даёт независимой защиты (тот же `JWT_SECRET + '_refresh'`) и нигде не используется бэкендом.
- Отсутствие `UNIQUE` на `phone` в SQLite (`users` таблица) → race condition при параллельной регистрации на один номер.

---

## Полные отчёты субагентов (для справки)

### 1. Платежи и подписки (денежный контур)

Файлы: `api/create-order.js`, `api/yookassa-webhook.js`, `api/cancel-subscription.js`, `api/cron-subscriptions.js`, `api/_lib/subscription-state.js`, `api/_lib/gift-windows.js`, `api/_lib/db.js`/`db-pg.js`.

**Критично:**
1. Подмена `X-Forwarded-For` обходила проверку подлинности вебхука (см. «Исправлено» выше).
2. Даже при успешной верификации через API использовались недоверенные поля из тела запроса (см. «Исправлено» выше).

**Важно:**
3. Гонка идемпотентности вебхука — нет `UNIQUE` на `yookassa_payment_id` (исправлено 2026-07-24, см. п.9 в «Исправлено» выше).
4. Перегруженное поле `tariff` между «текущий тариф подписки» и «разовая покупка амбассадора» → неверная сумма автосписания.
5. Гонка состояний в списании SHC-баллов — нет атомарности, нет защиты от отрицательного баланса.

**Рекомендации:**
6. Недостижимый код в `cron-subscriptions.js` (`tryRecurringPayment` всегда возвращает `pending: true`, ветка `success && !pending` мертва — на момент аудита это уже было на диске несохранённым изменением).
7. Расхождение между заявленным планом уведомлений (3 дня) и реализацией (только 1 день).
8. Избыточные debug `console.log` в горячих путях.
9. PII в логах `create-order.js`.
10. Дублирование `formatDate`, мёртвый экспорт `processCommissions`.

Положительно: SQL-инъекций не найдено, округление копеек корректно, бизнес-логика идентична между SQLite и PostgreSQL.

### 2. Авторизация, JWT, админка

Файлы: `api/_lib/auth.js`, `api/_lib/admin-auth.js`, `api/auth/*.js`, `api/_lib/supabase.js`, `api/admin/*.js`.

**Критично:**
1. Массовый отказ от проверки авторизации в пользовательских эндпоинтах — почти все действия выполнялись от лица `telegram_id`, присланного в теле запроса, без проверки против JWT (см. «Исправлено» выше — закрыто в этой сессии).
2. `api/export-contacts.js` — полностью открытый дамп базы пользователей (см. «Исправлено» выше).

**Важно:**
3. Захват аккаунта через email-OTP по номеру телефона (без подтверждения владения email или телефоном) — закрыто 2026-07-23 (см. п.5 в статусе исправлений выше); первичная привязка email для аккаунтов без пароля остаётся открытым архитектурным пробелом (нет SMS-верификации телефона).
4. `login-by-phone.js` — оракул для перебора номеров телефонов — не закрыто.
5. Fallback `JWT_SECRET` захардкожен в коде — не закрыто.
6. Refresh-токен не даёт дополнительной защиты и нигде не используется.
7. Отсутствие `UNIQUE`-ограничения на `phone` в SQLite → race condition при регистрации.

**Рекомендации:**
8. Сравнение пароля админа не constant-time, нет rate-limit на `admin-login.js`.
9. CORS с `Access-Control-Allow-Origin: '*'` на эндпоинтах с чувствительными данными (частично закрыто добавлением JWT-проверки в этой сессии, но CORS-политика сама по себе не сужена).
10. PII в логах.
11. OTP: анти-брутфорс работает только в рамках одного кода, а не телефона за период.

### 3. Фронтенд React

Файлы: `src/App.js`, `src/UserContext.js`, `src/DiscountShopPage.js`, `src/PaymentPage.js`, `src/ProfilePage.js`, `src/AdminPage.js`, компоненты корзины и профиля.

**Критично:**
1. Сломанная вкладка «Баннеры» в `AdminPage.js` — не закрыто.

**Важно:**
2. Несовпадение регистра ключа в `ProfilePage.js` — прогресс-бар подписки всегда `null` — не закрыто.
3. `promoCode` не сбрасывается после успешного заказа в `DiscountShopPage.js` — не закрыто.
4. Дублирование логики телефона в трёх файлах вместо `utils/phone.js` — не закрыто.

**Рекомендации:**
5. Отладочные `console.log` в `DiscountShopPage.js`.
6. Отсутствие мемоизации карточек товаров (`React.memo`, `useCallback`).
7. Эффект `IntersectionObserver` пересоздаётся при каждом `refetch` в `DiscountShopPage.js`.

Не найдено: `dangerouslySetInnerHTML`/XSS, гонок данных в `UserContext.js`/`useCartGifts.js`/`useWordle.js`.

### 4. Слой данных и каталоги

Файлы: `api/_lib/db.js`/`db-pg.js`, скрипты миграции/схемы PG, синхронизация каталогов.

**Критично:**
1. Таблица `orders` отсутствует во всех PG-схема-скриптах — не закрыто.

**Важно:**
2. Числовые PG-колонки без явного каста возвращаются как JS-строки (`getMonthRevenue`, `getTotalEarnings`) — не закрыто.

**Рекомендации:**
3. Синхронизация каталогов по имени не срабатывает на конкретных товарах из-за расхождений в написании — не закрыто.
4. Неатомарная запись каталогов в админке (`data/products/*.json`) — не закрыто.
5. `config/delivery-zone-overrides.json` — валиден, `normalizeStreet()` не заменяет «э→е» (в отличие от «ё→е») — мелкое замечание, не критично.

Положительно: экспорты `db.js` vs `db-pg.js` идентичны (63 функции), SQL-инъекций не найдено, рантайм-миграции ALTER TABLE согласованы по набору колонок.

### 5. Деплой и инфраструктура

Файлы: `Dockerfile`, `docker-compose.yml`, `nginx/`, `server.js`, `.dockerignore`, `.gitignore`.

**Критично:**
1. Утечка API-ключей TestSprite и Context7 (`opencode.jsonc`, коммит `827715f`) — закрыто 2026-07-23 (см. п.5 в статусе исправлений выше); ротация самих ключей остаётся на владельце.
2. Отсутствие защиты от двойного запуска крона (встроенный + HTTP endpoint) — закрыто 2026-07-23 (см. п.4 в статусе исправлений выше).

**Средне:**
3. Docker-контейнер запускается от root — не закрыто.
4. Отсутствуют security headers в Nginx (HSTS, X-Frame-Options, CSP и др.) — не закрыто.
5. Отсутствует rate limiting на критичных эндпоинтах — не закрыто.
6. `.dockerignore` не исключает `data/`, `*.db`, секреты — не закрыто.

**Низко:**
7. Порядок раздачи статики в `server.js` — корректен, замечаний нет.
8. Fallback `JWT_SECRET` — см. выше в разделе авторизации.
9. Нет backup-стратегии для persistent volume `app-data` — не закрыто.
